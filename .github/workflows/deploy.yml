steps:
  - name: Deploy
    env:
      API_TOKEN: ${{ secrets.MY_API_TOKEN }}  # Используем секрет
    run: |
      echo "Токен доступен как переменная окружения: $API_TOKEN"
      # Пример: curl -H "Authorization: Bearer $API_TOKEN" https://api.example.com
